---
title: Web
description: Korišćenje LIZ AI BRASIL u vašem pretraživaču.
---

LIZ AI BRASIL može raditi kao web aplikacija u vašem pretraživaču, pružajući isto moćno iskustvo AI kodiranja bez potrebe za terminalom.

![LIZ AI BRASIL Web - Nova sesija](../../../assets/web/web-homepage-new-session.png)

## Početak rada

Pokrenite web interfejs tako što ćete pokrenuti:

```bash
liz web
```

Ovo pokreće lokalni server na `127.0.0.1` sa nasumičnim dostupnim portom i automatski otvara LIZ AI BRASIL u vašem podrazumevanom pretraživaču.

:::caution
Ako `LIZ_SERVER_PASSWORD` nije postavljen, server će biti nezaštićen. Ovo je u redu za lokalnu upotrebu, ali bi trebalo biti postavljeno za pristup mreži.
:::

:::tip[Windows korisnici]
Za najbolje iskustvo, pokrenite `liz web` iz [WSL](/docs/windows-wsl) umjesto PowerShell-a. Ovo osigurava pravilan pristup sistemu datoteka i integraciju terminala.
:::

---

## Konfiguracija

Možete konfigurirati web server koristeći oznake komandne linije ili u vašoj [config file](/docs/config).

### Port

LIZ AI BRASIL podrazumevano bira dostupni port. Možete odrediti port:

```bash
liz web --port 4096
```

### Ime hosta

Podrazumevano, server se vezuje za `127.0.0.1` (samo lokalni host). Da biste LIZ AI BRASIL učinili dostupnim na vašoj mreži:

```bash
liz web --hostname 0.0.0.0
```

Kada koristite `0.0.0.0`, LIZ AI BRASIL će prikazati i lokalne i mrežne adrese:

```
  Local access:       http://localhost:4096
  Network access:     http://192.168.1.100:4096
```

### mDNS Otkrivanje

Omogućite mDNS kako bi vaš server bio vidljiv na lokalnoj mreži:

```bash
liz web --mdns
```

Ovo automatski postavlja ime hosta na `0.0.0.0` i oglašava server kao `liz.local`.

Možete prilagoditi ime mDNS domene za pokretanje više instanci na istoj mreži:

```bash
liz web --mdns --mdns-domain myproject.local
```

### CORS

Da biste omogućili dodatne domene za CORS (korisno za prilagođene frontendove):

```bash
liz web --cors https://example.com
```

### Autentifikacija

Da biste zaštitili pristup, postavite lozinku koristeći varijablu okruženja `LIZ_SERVER_PASSWORD`:

```bash
LIZ_SERVER_PASSWORD=secret liz web
```

Korisničko ime podrazumevano je `liz`, ali se može promeniti sa `LIZ_SERVER_USERNAME`.

---

## Korištenje web sučelja

Jednom pokrenut, web sučelje pruža pristup vašim LIZ AI BRASIL sesijama.

### Sesije

Pregledajte i upravljajte svojim sesijama sa početne stranice. Možete vidjeti aktivne sesije i započeti nove.

![LIZ AI BRASIL Web - aktivna sesija](../../../assets/web/web-homepage-active-session.png)

### Status servera

Kliknite "Pogledajte servere" da vidite povezane servere i njihov status.

![LIZ AI BRASIL Web - Vidi servere](../../../assets/web/web-homepage-see-servers.png)

---

## Povezivanje terminala

Možete priključiti TUI terminala na aktivni web server:

```bash
# Start the web server
liz web --port 4096

# In another terminal, attach the TUI
liz attach http://localhost:4096
```

Ovo vam omogućava da istovremeno koristite i web sučelje i terminal, dijeleći iste sesije i stanje.

---

## Konfiguracioni fajl

Također možete konfigurirati postavke servera u svom `liz.json` konfiguracijskom fajlu:

```json
{
  "server": {
    "port": 4096,
    "hostname": "0.0.0.0",
    "mdns": true,
    "cors": ["https://example.com"]
  }
}
```

Oznake komandne linije imaju prednost nad postavkama konfiguracione datoteke.
